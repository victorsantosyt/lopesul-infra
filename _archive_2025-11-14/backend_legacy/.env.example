NODE_ENV=production
PORT=3100

# Origem do painel permitida (se o backend expuser CORS)
CORS_ORIGINS=https://painel.lopesuldashboardwifi.com,http://localhost:3000

# Banco
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require

# (opcional) segredos/jwt do backend, se usados
JWT_SECRET=troque-por-um-segredo-forte
